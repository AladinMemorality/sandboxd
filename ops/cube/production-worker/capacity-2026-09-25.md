# Current capacity review — 2026-09-25

The installed policy is **four active/waking/creating2CPU/2GiB guests**. Native
quota is10000mCPU/10GiB, paused release1.0, total VM ceiling128, and one create
workflow. The worker remains40GiB/12vCPU. The historical12-slot experiment is
not the current rollout limit. The repeated four-app workload on the durable
worker passed; see `../workload/four-app-durable-validation-2026-09-25.json`.
The new disk guard still requires its own live acceptance.

Read-only measurements around14:58–15:03UTC, before any later host enrollment:

| Quantity | Observed |
| --- | ---: |
| Production projects / raw ready published snapshots |66 /31 |
| Worker `/data` XFS size |343471554560bytes (319.883GiB) |
| Worker `/data` used / available |45918928896 /297552625664bytes |
| Outer NVMe available |416176295936bytes (387.594GiB) |
| NVMe worker data.qcow2 allocated |~39.83GiB |
| Measured owner app + preserved home |26213120679bytes (~24.414GiB) |
| Separately retained home/history |326011796bytes (~0.304GiB) |
| Largest app+home pair |1670603070bytes (~1.556GiB) |
|31 existing raw library directories allocated |4294983680bytes (~4.000GiB) |

Counts and source sizes are an unfrozen observation, not a cutover inventory.
Eight exact reviewed template RAM files are2GiB logical each; their sparse blocks
currently use much less. Eight writable layers have10GiB logical capacity each.
The existing42.76GiB filesystem-used baseline includes those template artifacts,
control data, retained independent captures and historical orphan storage. Do
not add the templates again to this baseline or remove the historical orphan
`1a1474444e064d6f8da340f324f4fd7f` to make the projection look better.

A conservative first-fleet projection is42.76GiB baseline +132GiB for66 full RAM
snapshots +24.414GiB app/home +0.304GiB retained history = about199.48GiB before
ongoing growth, import staging, concurrent pause writes and unrelated maintenance.
The existing65% scheduler cutoff on320GiB is about207.92GiB: only8.45GiB of
headroom remains. **The present disk is not a comfortable global fleet budget.**

The proposed448GiB virtual data expansion leaves about91.7GiB beneath a65%
cutoff after that projection. This document does not claim expansion executed.
Outer free387.6GiB plus current39.83GiB allocated cannot fully back448GiB if every
logical extent fills. Therefore inner growth alone is insufficient: independent
outer/inner free-space checks and finite durable admission debits are required.
See `../storage-guard/README.md` for the prepared96GiB observation threshold,
48GiB post-debit reserve,12GiB per allocation and monotonic epoch accounting.

The native scheduler checks cached filesystem percentages, not66×10GiB logical
reservations. XFS origin-pinned resume bypasses the Master placement filter;
Cubelet's resume admission checks CPU/RAM. The reviewed controller guard must
cover create/connect, paused-delete and recovery replacement. Direct management
credentials remain operator-only. The65% filter remains an independent
conservative limit, not a synchronous guarantee of48GiB free space.

Migration should preserve a fresh frozen inventory and originals, then process
one project at a time: create with the shared guarded client, import private
app/home, verify current data/config/history, acknowledge durable provider pause,
and only then commit the stable binding. This releases its active slot while
retaining its stored files/RAM. Four active slots do not imply66 simultaneously
running apps. Always-on apps consume slots and must stay represented.

The31 raw published cards remain controller-side immutable source directories;
remix exports their reviewed app scope lazily. They do not each require a new
2GiB Cube RAM snapshot. Their library retention/backup is separate from worker
space.660GiB aggregate per-project disk entitlement remains thin provisioned;
future user growth can correctly refuse new wakes/allocations before all logical
quotas fill. Imports stage inside the same10GiB guest disk and may reject ENOSPC;
no protocol-limit promise of simultaneously fitting two8GiB expansions is made.

No host routes, disks, quotas, guests or services were changed during this review.
