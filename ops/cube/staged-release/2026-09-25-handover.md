# Cube cutover handover, 2026-09-25

This records scoped production progress; it does not approve global migration.
Historical receipts remain retained. The earlier19:33 observation (Cube disabled,
fixture without a sandbox) is superseded by the completed one-app enrollment and
runtime release below. The user subsequently handed over VPS writes; each actual
maintenance/release still uses the shared deployment/operator locks.

## Current verified production

Independent read-only inspection at22:05:43UTC confirmed runtime revision
`1fcfde24a08b02d7087991fb14fe23060bcf36e3`, running controller
`93169a6a71c6d1e35785683418e0c0421674b2fc48baaf973005d542f9c11914`, image
`sha256:74f76d6b6db87b4c710c4c4fbf57db463e15e16f30be6e2abb60ee11fd5119ee`.
Cube is enabled **only** for owned app `01M3CZB4HXT2Y8HP8CEY75PCWY`; its stable
sandbox is `01M3D1Q0E1KM1FEM244XVHEC65`. The four-active-slot admission contract
preserves2CPU/2GiB per guest and the configured physical-storage guard. Both
management relays are healthy in the exact current controller network namespace.
The preview signing key is persistent through
`SANDBOXD_ENV_FILE=/var/lib/sandboxd/cube-preview-auth.env`; no value is published.

Inventory observed69 app records,68 Docker sandboxes, one Cube binding and zero
active tasks/pending admission/pending recovery. These are observations, not
future execution pins. Existing customer projects remain Docker-backed.

The release unit `cube-runtime-release-20260925-01` completed successfully with
exit0. Its exact-SHA version, consistent SQLite backup, Vite image acceptance
(three cold reload/shutdown cases), PostgreSQL acceptance (four tests including
real committed-data crash/restart/cold-backup behavior) and success receipt are
retained in the private release directory. These image checks do not prove the
full production paired backup or customer migration.

Actual post-release private preview, file read, PostgreSQL health, owner access
and private screenshot checks passed21:50:06–21:50:13UTC. Capture took4.065s;
owner read matched the actual JPEG, foreign and anonymous reads were denied.
Root independently viewed that exact JPEG: notes heading, note input/Save button
and empty state rendered, with no login/error page. This proves rendered-page
capture, not successful save/search feature completion or AI coding quality.
The screenshot was created through a separate journal; all four failed AI tasks
and the original pending AI journal were preserved unchanged.

See [sanitized release evidence](runtime-1fcfde2-2026-09-25.json) for exact
receipt hashes and narrow results. No credentials, cookie/token URLs, source
contents, prompts, session journals or screenshot bytes are committed.

## Next scoped release (prepared, not deployed by this review)

Candidate `5fdb953dea1aafaf486b5e3fdd63e20de2781a89` updates the bounded Motion
service contract for handed-over actions and consent. Exact-head CI
[36194231117](https://github.com/AladinMemorality/sandboxd/actions/runs/36194231117)
passed all six jobs. Migrations, image source and deployment code are unchanged
from the installed release. It still requires exact image build/acceptance and
a fresh root-private review pinning current controller, one-app scope, runtime
override and rendered Compose hash. Standard unattended allowlist deployment
remains refused. A prepared local review is not a deployment or scope expansion.

The reviewed deployer successfully used for the last release is separately
staged; the old installed `/opt/sandboxd/deploy.sh` remains unchanged. Do not
silently invoke the old script or treat a consumed review as reusable.

## Preparation and remaining gates

- Storage observer and schema34 lifecycle coordinators are installed; successful
  empty-worker stop/start and later controller-pin refresh have separate receipts.
- Four-app workload and fifth-allocation refusal passed with verified cleanup.
  This is bounded synthetic capacity, not proof that all customer workloads fit.
- The old immutable image archive has five images and50 verified content blobs,
  1,450,068,480 bytes, SHA256
  `c54a4cb25ff61cec955dd4e0c1ca1656f4426b52c8348b49e565bb6db931b2bb`.
  Its controller image is obsolete after release. Final backup roles must include
  the final installed controller image/config/key and fresh canonical inventory.
- The separate operator recovery-data fixture is prepared and locally tested.
  It must follow frontend benchmark manifest restoration and final controller
  repinning. Operator-authored markers/SQL rows can prove recovery independently
  of model quality; failed AI tasks must remain failed, with history preserved.
- Complete the consistent full paired backup, encrypted independent copy/readback
  and actual isolated application/history/SQL restore. Include closed runtime
  metadata/key, worker disks, Docker homes/history/PG, library, platform database,
  separate Motion state and upload storage authority. Original and restored
  40GiB workers must run in separate host-capacity windows.
- Finish real Motion worker/template/installer compatibility and actual normal
  coding acceptance. Source unit tests or preview health alone are insufficient.
- Refresh the entire fleet plan, including projects added since earlier reports;
  preserve IDs, private access and links, verify each migration/rollback, then
  enable global creation only after acceptance. Changing the default alone does
  not migrate stored sandboxes. Preserve the tested four-slot active limit.

Global activation, full paired backup, independent full restore and AI coding
acceptance remain **unproven** by this release. Screenshot storage-deletion IAM
cleanup remains deferred at the user's request.
