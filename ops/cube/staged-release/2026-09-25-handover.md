# Cube cutover handover, 2026-09-25

This is an operational status record, not deployment acceptance. It supplements
the earlier staged-release plan; historical evidence remains unchanged.

## Current production

Read-only inspection at approximately 19:33 UTC confirmed runtime revision
`6b4fd4d054dbf8247287c9edb7d7e87ce9668e9a`, platform revision
`1b4e9ac2a02dde0e51021dbe09898548860df4a1`, and controller
`54f9d073a7c2739b55481d5bbbb80f22847cb1e1d3225fe5af9590f2f48de5f3`.
The controller image remains
`sha256:14e9b9244729875d15b95082c100b61582c929b48deba1ecd60b38729f344f82`.
Cube is disabled; controller health and readiness both returned HTTP 200.
The latest inventory contains 69 apps and 68 Docker sandboxes, including the
owned fixture app without a sandbox. These counts are observations, not pins
for a future execution.

The user reports another session is still deploying. Motion Studio's current
installer bypasses the deployment lock and writes directly into a Docker home.
Consequently the prepared single-app cutover must not run until those writes
have stopped and the coordinating operator has the handover. The previously
passing `cube-controller-cutover-check-04` inventory is stale. Refresh exact
revisions, containers, app counts, routing and configuration hashes, then run a
new preflight. An idle process snapshot is not a writer handover.

## Preparation completed

- Storage observer and schema-34 lifecycle coordinators are installed. Their
  independent receipts are recorded under the corresponding operational tools.
- The guarded four-app workload passed, including fifth-allocation refusal and
  verified cleanup. See `../workload/guarded-live-2026-09-25.json`; this is a
  synthetic capacity test, not migrated customer acceptance.
- Five immutable images (four runtime bases and the current controller) were
  exported to a private staging directory. The 1,450,068,480-byte archive has
  SHA256 `c54a4cb25ff61cec955dd4e0c1ca1656f4426b52c8348b49e565bb6db931b2bb`.
  All five manifests and 50 content-addressed blobs passed verification, and a
  separate full-file read reproduced the hash. No customer homes or databases
  were captured; this is not the full paired backup.
- Runtime deployment changes `1ec0701` and `ecae5d9` refresh the coordinator's
  controller identity after a verified release and reject candidate migration
  changes before live activation. The 23 deployment contract tests passed.
  These changes have not been installed on production.

## Work still required before global activation

1. Finish and review durable project creation recovery when Cube capacity is
   busy. The same request must preserve its app, prompt and uploads, and must
   never blindly repeat an ambiguous paid task submission. Test real PostgreSQL
   claim contention, owner isolation and recovery alongside client behavior.
2. Finish Motion Studio compatibility: its host-worker IP/MAC allowance and
   direct workspace installer do not work in Cube. Use a dedicated app- and
   generation-bound service adapter, explicit upload/media range limits, and a
   provider-aware installer. Preserve its independently stored worker data and
   account for its separate memory/CPU allocation. Local preview health alone
   does not prove worker access.
3. After handover, enroll only the owned fixture through the reviewed controller
   cutover. Run the actual initial task, scoped private screenshot, metering,
   history and optional PostgreSQL persistence checks on that final topology.
4. Complete a consistent paired backup and independent restore. Include closed
   runtime metadata/key, worker disks, all Docker homes/history/databases,
   library artifacts, platform database and separate application worker state.
   Run original and restored 40-GiB workers in separate windows on this host.
5. Refresh the full fleet plan, preserve existing IDs and links, migrate and
   verify each project, then enable global Cube creation and monitor production.
   Keep the tested four-active-slot limit and idle pausing. Merely changing the
   default does not migrate existing sandboxes.

The prepared screenshot storage-deletion IAM cleanup remains deferred at the
user's request. It does not turn any incomplete Cube acceptance into a pass.
