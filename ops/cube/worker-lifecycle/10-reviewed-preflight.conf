# Install only on the explicit reviewed nested SERVICES list, never arbitrary glob.
# Target ordering alone does not stop Wants dependencies from starting early.
[Unit]
Requires=baarcha-cube-preflight.service
After=baarcha-cube-preflight.service
