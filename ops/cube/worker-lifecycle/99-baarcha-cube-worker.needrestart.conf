# Cube worker restarts require the coordinated guest drain and clean-stop proof.
# Keep reporting outdated processes, but do not select this VM for apt restarts.
$nrconf{override_rc}->{qr(^baarcha-cube-worker-01(?:\.service)?$)} = 0;
