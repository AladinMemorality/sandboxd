/* Starter placeholder — replace this component entirely on the first build. */
export default function App() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <div className="text-center">
        <div className="mx-auto mb-4 h-2 w-16 animate-pulse rounded-full bg-muted-foreground/30" />
        <p className="text-sm text-muted-foreground">
          This sandbox is ready. The app will appear here as it is built.
        </p>
      </div>
    </div>
  )
}
